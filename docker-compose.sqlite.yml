# Override for SQLite (no PostgreSQL needed).
# Usage: docker compose -f docker-compose.yml -f docker-compose.sqlite.yml up -d
services:
  db:
    # Disable the PostgreSQL service
    profiles: ["disabled"]

  vigil:
    environment:
      DATABASE_URL: ""  # Empty = fall back to SQLite in /data
    depends_on: []
