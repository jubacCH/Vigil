services:
  db:
    image: postgres:16-alpine
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-vigil}
      POSTGRES_USER: ${POSTGRES_USER:-vigil}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-vigil}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-vigil}"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  vigil:
    build: ./backend
    container_name: vigil
    ports:
      - "8000:8000"
      - "514:1514/udp"    # Syslog UDP (host 514 → container 1514)
      - "1514:1514/tcp"   # Syslog TCP
    volumes:
      - ./data:/data
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-vigil}:${POSTGRES_PASSWORD:-vigil}@db:5432/${POSTGRES_DB:-vigil}
      DATA_DIR: /data
      SYSLOG_PORT: "1514"
    depends_on:
      db:
        condition: service_healthy
    cap_add:
      - NET_RAW   # Required for ICMP ping
    restart: unless-stopped

volumes:
  pgdata:
