# Vigil environment configuration
# Copy to .env and adjust values

# PostgreSQL (used by docker compose)
POSTGRES_DB=vigil
POSTGRES_USER=vigil
POSTGRES_PASSWORD=changeme

# App database URL (set automatically by docker compose)
# For SQLite (default if not set): leave empty
# For PostgreSQL: postgresql+asyncpg://vigil:changeme@db:5432/vigil
DATABASE_URL=postgresql+asyncpg://vigil:${POSTGRES_PASSWORD}@db:5432/vigil

# Secret key for encryption (auto-generated if not set)
# SECRET_KEY=

# Data directory (for SQLite DB + secret key file)
# DATA_DIR=/data
